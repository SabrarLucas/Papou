{% extends 'base.html.twig' %}

{% block body %}

    <h1>product</h1>
    <ul>
        {% for product in products.data %}

        <li><a href="{{ path('detail' , {'id': product.id}) }}">{{ product.name }}</a></li>
        
        {% endfor %}
    </ul>
    {% include "partials/paginationProductAll.html.twig" %}
{% endblock %}