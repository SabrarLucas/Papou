{% extends 'base.html.twig' %}

{% block title %}Profil{% endblock %}

{% block body %}
<h1>Profil</h1>

{{ form_start(form) }}
    {{ form_row(form.lastname) }}
    {{ form_row(form.firstname) }}
    {{ form_row(form.address) }}
    {{ form_row(form.city) }}
    {{ form_row(form.zipcode) }}
    {{ form_row(form.email) }}
    <a href="{{ path('app_user_profil_order', {'id': app.user.id}) }}">mes achat</a>
    <button type="button"><a href="{{ path('main') }}">retour</a></button>
    <button type"submit">valider</button>
    <button type="button"><a href="{{ path('app_user_profil_delete', {'id': app.user.id}) }}">supprimer mon compte</a></button
{{ form_end(form) }}

<a href="{{ path('app_logout') }}">logout</a>
{% endblock %}